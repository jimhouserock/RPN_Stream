# Environment Variables Example for Postfix Calculator
# Author: Jimmy Lin
# 
# SECURITY NOTE: This is an example file. Never commit actual secrets to Git!
# Copy this file to .env and fill in your actual values for local development.

# Rails Environment
RAILS_ENV=production

# Static File Serving (for containerized deployment)
RAILS_SERVE_STATIC_FILES=true

# Logging (for container environments)
RAILS_LOG_TO_STDOUT=true

# Secret Key Base (REQUIRED for production)
# Generate with: openssl rand -hex 64
# SECRET_KEY_BASE=your-64-character-secret-key-here

# Optional: Force SSL for custom domains
# RAILS_FORCE_SSL=true

# Optional: Custom Port (default: 3000)
# PORT=3000

# Database URL (if using external database instead of SQLite)
# DATABASE_URL=postgresql://user:password@host:port/database

# Redis URL (if using Redis for caching/sessions)
# REDIS_URL=redis://localhost:6379/0

# IMPORTANT SECURITY NOTES:
# 1. Never commit .env files to Git
# 2. Use different secrets for each environment
# 3. Rotate secrets regularly
# 4. Use environment-specific secret management in production
